using System;
using Xunit;

namespace ConversionToolTests
{
    public class APIEdgeTests
    {
        [Fact]
        public void PreIntegrationTest1()
        {
            /* Test API results with Mock objects - can be performed before actual API is completed. 
             * This is performed by wrapping the api logic and abstracting it => ConverterAPI 
             * But I did not write these tests.
             */
        }
    }
}
